# API-trip-generator
Flask App that returns x day trip, given city and days parameters with the openai API

source env/bin/activate

pip freeze > requirements.txt

deploy on gcloud -> gcloud run deploy --source .
